pipeline{
    agent any

    stages {
        // 1. compile
        stage('compile') {
            steps {
                echo "compile ..."
            }
        }

        // 2. test
        stage('test') {
            steps {
                echo "test ..."
            }
        }

        // 3. pack
        stage('pack') {
            steps {
                echo "pack ..."
            }
        }

        // 4. deploy
        stage('deploy') {
            steps {
                echo "deploy ..."
            }
        }
    }
}